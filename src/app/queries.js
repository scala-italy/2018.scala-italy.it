// import { Query, t } from 'revenge';

export default (/* API */) => {

  // const user = Query({
  //   id: 'user',
  //   cache: 'manual',
  //   cacheParams: {
  //     token: t.String
  //   },
  //   fetch: ({ token }) => () => API.getUser({ token })
  // });

  return {
    // user
  };
};
